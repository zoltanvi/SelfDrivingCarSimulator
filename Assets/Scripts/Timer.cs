using UnityEngine;
using TMPro;

public class Timer : MonoBehaviour {

	//[SerializeField] private TextMeshProUGUI countdownText;
	//float timeLeft = 15.0f;

	//void Update()
	//{
	//	countdownText.text = string.Format("{0:0.00}", timeLeft);
	//	timeLeft -= Time.deltaTime;
	//	if (timeLeft < 0)
	//	{
	//		for (int i = 0; i < CarGameManager.Instance.cars.Length; i++)
	//		{
	//			CarGameManager.Instance.cars[i].gameObject.GetComponent<CarController>().ResetThisCar();
	//		}
	//		timeLeft = 15.0f;
	//	}
	//}
}
